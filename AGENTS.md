# 夺锦之星比赛代码开发守则

本文件是 `duojin_ws` 的最高优先级仓库内开发规范，主要面向 AI 编程代理，也适用于人工开发。除非用户在当前任务中明确覆盖某条规则，否则所有分析、改动、测试和交付都必须遵守本文件。

规范关键词：

- **必须 / 禁止**：不可跳过的硬约束。
- **应当**：默认执行；不执行时必须说明理由。
- **可选**：按任务需要采用。

## 1. 项目目标与优先级

本项目为 R1 Lite 机器人参加世界人形机器人运动会 3.1 零售服务岗的赛队 ROS 2 overlay。比赛主流程包括商品拣选、缺货补货和乱放商品归位。

工程优先级固定为：

1. 人员、机器人和场地安全；
2. 行为正确、失败可停止、状态可恢复；
3. 可重复完成并可用数据验收；
4. 模块边界清晰、代码可维护；
5. 完成速度和运行性能；
6. 算法或模型的新颖程度。

不得为了“看起来智能”牺牲确定性。有限 SKU、固定场地和明确流程优先使用可解释的检测、模板化抓放、航点导航和有限状态机；大模型或 VLA 不得绕过安全层直接控制执行器。

## 2. 已确认的系统边界

```text
本地开发机
  只编辑源码、运行不依赖 ROS 的测试、做静态检查和 Git 操作

机器人侧 ~/galaxea/install_430
  厂商 SDK underlay；启动 HDAS、Mobiman、Joint Tracker、Relaxed IK、相机等

机器人侧 ~/duojin_ws
  赛队 overlay；只实现感知、定位、导航、操作、任务编排和诊断
```

必须遵守以下边界：

- `install_430` 固定为只读 underlay。禁止修改、复制代码进去、提交其生成物或通过 `sys.path` 引用绝对目录。
- `duojin_ws` 不得重复启动厂商已经管理的 HDAS、Joint Tracker、Relaxed IK、Mobiman 或相机节点。
- 项目通过 ROS 2 Topic、Service、Action 和 TF 使用 SDK 能力；不得直接操作厂商内部实现。
- 赛队程序只向已确认的 `/motion_target/*` 接口发目标，从 `/hdas/*` 等反馈接口读状态。禁止直接向 `/motion_control/*` 发布 `MotorControl`。
- 环境加载顺序固定为 SDK underlay 后项目 overlay：

  ```bash
  source ~/galaxea/install_430/setup.bash
  source ~/duojin_ws/install/setup.bash
  ```

- 本地没有 ROS 2 Humble 是已知条件。不得把本地无法 `colcon build` 误判为代码缺陷，也不得声称完成了未在工控机执行的 ROS 构建或真机验证。
- `build/`、`install/`、`log/` 是工控机生成物，不得手工编辑或提交。

项目的现有事实先从 `README.md`、`docs/项目使用说明.md`、`docs/r1_lite_interfaces.md` 和相关包 README 获取。文档与真机不一致时，记录差异并以可复现的真机证据为准，不能静默猜测。

### 2.1 本地与工控机路径语义

文档、代码和命令必须明确自己运行在哪台机器上。当前固定映射为：

| 含义 | 本地开发机 | R1 Lite 工控机 |
| --- | --- | --- |
| 用户主目录 | `/home/vedal` | `/home/r1lite`，即工控机命令中的 `~` 和 `${HOME}` |
| 赛队源码 | `/home/vedal/WorkStation/世界人型机器人运动会/duojin_ws` | 默认 `${HOME}/duojin_ws`，即 `/home/r1lite/duojin_ws` |
| 厂商 SDK | `../galaxea/install_430`，只读接口快照 | `${HOME}/galaxea/install_430`，即 `/home/r1lite/galaxea/install_430` |
| 项目构建产物 | 不应存在或使用 | `<赛队源码>/build`、`<赛队源码>/install`、`<赛队源码>/log` |

路径规则是硬约束：

- `README`、runbook 和给操作员的命令若使用 `~`，必须明确标注为“在工控机执行”；这里的 `~` 只能解释为 `/home/r1lite`。
- Shell 代码不得依赖调用者当前目录。项目根目录必须从 `${BASH_SOURCE[0]}` 所在位置推导；SDK 和工控机主目录资源使用 `${HOME}`，不要在引号中写不会展开的字面量 `~`。
- 除明确负责部署到机器人账户的脚本外，源码不得硬编码 `/home/r1lite`。这样 `duojin_ws` 即使不在默认目录，项目内部路径仍然有效。
- 任何将要部署的源码、配置、launch 和脚本都不得出现 `/home/vedal` 或 `WorkStation`；这些只属于本地参考路径。
- ROS 包运行时读取 config、launch、模型索引等资源，必须通过包 share 目录或显式 ROS 参数定位，禁止使用 `Path.cwd()`、`../config` 等依赖启动目录的路径。
- 仓库内文档引用以仓库根目录为基准，例如 `docs/...`；ROS 包内部资源以该包目录为所有权边界，并在安装规则中显式安装。
- AI 给出命令时必须分成“本地开发机执行”和“工控机执行”两组；不得让用户猜命令运行位置。
- 新功能规格必须说明最终运行位置。真正的 ROS 构建、SDK 启动、ROS 图检查和运动测试一律属于工控机步骤。

## 3. 每次任务的强制开发流程

任何新增功能、行为修改或真机问题修复都按本节顺序推进。禁止从一句“做 YOLO 抓取”直接跳到大段实现。

### 3.1 先建立任务契约

开始编码前，必须用可验收语言回答：

- 最终场景是什么，谁触发，何时算成功？
- 输入是什么，来自哪个节点、话题、文件或操作员？
- 输出是什么，发给谁，坐标系、单位和频率是什么？
- 开始前必须满足哪些状态？
- 正常流程有哪些离散步骤？
- 每一步如何观测成功或失败？
- 超时、无数据、低置信度、不可达、抓空和掉落时如何处理？
- 哪些行为明确不属于本次任务？
- 用哪些数值指标验收？

涉及以下任一条件时，必须先从 `docs/templates/feature-spec.md` 创建 `docs/features/<feature-id>.md`：

- 新增 ROS 包或跨两个以上包；
- 新增或更改机器人运动行为；
- 新增 SDK 接口、相机/TF 链路或模型；
- 改变任务状态机、恢复策略或比赛主流程；
- 预计不止一个小提交才能完成。

`feature-id` 使用小写短横线，例如 `sku-visual-grasp`。单纯文档修正或局部无行为重构可以不建功能规格，但仍要在交付说明中写明验收条件。

### 3.2 把目标拆成单一能力

每个能力必须只有一个清晰职责，能够独立给输入、观察输出和判定结果。推荐格式：

| 字段 | 要求 |
| --- | --- |
| ID | 稳定编号，例如 `CAP-PER-01` |
| 职责 | 一个动词短语，只做一件事 |
| 输入 | 类型、单位、坐标系、有效范围 |
| 输出 | 类型、单位、坐标系、有效范围 |
| 前置条件 | 节点、标定、反馈、安全状态 |
| 成功判据 | 可测量，不得只写“正常工作” |
| 失败结果 | 明确错误码或状态，不用模糊布尔值吞掉原因 |
| 验证层级 | 本地、工控机 dry-run、单模块真机或全流程 |

例如“YOLO 识别物体抓取”至少应拆为：

```text
获取并同步 RGB-D 帧
→ 识别指定 SKU
→ 过滤无效深度并求相机坐标三维位置
→ 经已验证 TF 转换到机器人基座坐标系
→ 判断置信度、时效、可达性和碰撞余量
→ 生成预抓取/抓取/抬升/撤回目标
→ 在执行许可下完成夹爪和机械臂动作
→ 用夹爪/视觉/位置反馈验证抓取
→ 成功返回，或按原因重试、换抓点、跳过、安全停止
```

“机械臂向某位置移动”仍然太粗。必须继续明确当前位置来源、目标坐标系、米/弧度单位、绝对或相对目标、插值方式、速度限制、发布频率、到位误差、超时、碰撞条件和停止行为。

### 3.3 先找 SDK 能力证据，再写适配代码

查接口时按以下证据优先级工作：

1. 本仓库已经经过真机联调的接口和测试记录；
2. 工控机当前 ROS 图、`topic info -v`、`node info`、单帧反馈和日志；
3. 与工控机同版本的 `install_430` 配置、消息定义和 launch 文件；
4. 厂商官方文档；
5. 推测或通用 ROS 经验——只能形成待验证假设，不能进入运动代码。

新增 SDK 依赖前必须记录：

- 接口名称和方向；
- 完整消息/服务/动作类型；
- 字段语义、单位、范围和数组长度；
- 坐标系和时间戳语义；
- QoS、期望频率和新鲜度上限；
- 必需节点和启动方式；
- 控制权归属，是否可能有第二个发布者；
- 证据文件或真机命令；
- 状态：`documented`、`observed` 或 `robot-verified`。

新领域接口写入 `docs/interfaces/<domain>.md`。禁止把尚未验证的接口写成“已确认”，禁止仅凭话题名猜消息方向、QoS 或字段含义。

### 3.4 先决定所有权，再创建文件

编码前必须确定：

- 功能属于哪个包；
- 纯算法、ROS 适配、配置、launch 和测试分别放哪里；
- 是否已有同职责模块可以扩展；
- 依赖方向是否符合第 4 节；
- 是否会出现第二个真相源、第二个控制节点或循环依赖。

没有明确所有权的新文件不得创建。禁止先把代码放在根目录或 `utils.py`，以后再整理。

### 3.5 按最小纵向闭环实现

实现顺序应当让每一步都能独立证明：

1. 定义输入、输出、错误和配置；
2. 实现不依赖 ROS 的纯逻辑，并在本地测试；
3. 实现薄 ROS 适配层；
4. 接入只读反馈和 dry-run，先证明数据链；
5. 加入执行许可、安全门和明确停止路径；
6. 单能力低风险真机验证；
7. 接入上一级状态机；
8. 最后进行完整比赛流程回归。

一次改动只解决一个能力或一个明确故障。真机问题定位时一次只改变一个主要变量，不得同时改 QoS、频率、坐标、控制器和动作参数后用一次结果猜原因。

### 3.6 分级验证，不得跨级

| 层级 | 环境 | 必须证明的内容 |
| --- | --- | --- |
| L0 | 本地静态 | 格式、语法、导入边界、配置 schema、Shell 语法 |
| L1 | 本地单元 | 纯算法、状态转换、边界值、超时与失败分支 |
| L2 | 工控机构建 | 在 `install_430` underlay 下 `colcon build --symlink-install` 成功 |
| L3 | 工控机只读/dry-run | 节点可启动，输入真实，输出目标合理，不发运动命令 |
| L4 | 单子系统真机 | 清场、急停在手、低速小范围、单次能力闭环通过 |
| L5 | 多模块集成 | 感知、决策、导航/操作接口和恢复链路通过 |
| L6 | 比赛回归 | 按赛制顺序、计时、重复多轮并记录成功率与扣分事件 |

不得因为 L0/L1 通过就宣称功能完成；没有工控机或机器人时，必须准确写“完成到 L1，L2–L6 待验证”。每次 L4–L6 测试都从 `docs/templates/robot-test-record.md` 生成记录。

### 3.7 完成定义

功能只有同时满足以下条件才可标记完成：

- 任务契约中的验收指标有证据；
- 正常路径与主要失败路径均被测试；
- 机器人运动具备执行许可、限幅、超时和安全停止；
- 新接口、参数、launch 和操作步骤已文档化；
- 没有修改厂商 SDK 和生成目录；
- 没有未解释的新依赖、重复实现或跨层导入；
- Git 差异只包含本任务文件；
- 清楚标记了尚未执行的验证层级。

## 4. 固定目录与依赖规则

目标结构如下；目录在首次出现真实内容时创建，禁止为了“看起来完整”提交空目录：

```text
duojin_ws/
├── AGENTS.md                         AI/人工开发唯一守则
├── CLAUDE.md                         指向 AGENTS.md 的兼容入口
├── README.md                         项目定位与最短使用入口
├── start.sh / stop.sh                整机环境生命周期入口
├── src/
│   ├── duojin_interfaces/            仅赛队自定义 msg/srv/action
│   ├── duojin_robot_interface/       SDK 话题/服务/动作适配与安全门
│   ├── duojin_perception/            SKU、清单、货架、RGB-D 与 TF 定位
│   ├── duojin_navigation/            航点导航和货架精确停靠
│   ├── duojin_manipulation/          抓取、放置、验证和恢复原语
│   ├── duojin_task_manager/          比赛状态机与重试策略
│   ├── duojin_bringup/               launch 和跨模块参数装配
│   └── duojin_arm_test/              现有机械臂接口验证包，仅作测试/诊断
├── docs/
│   ├── architecture/                 架构决策与模块边界
│   ├── features/                     按任务建立的功能规格
│   ├── interfaces/                   SDK/ROS 接口证据表
│   ├── runbooks/                     构建、部署、运行、故障处理
│   ├── test-records/                 真机和整场回归记录
│   └── templates/                    规格与测试记录模板
└── scripts/                           仅构建、部署、启动、诊断等运维入口
```

### 4.1 包职责

- `duojin_robot_interface` 是厂商 SDK 的唯一适配边界。生产包不得在各处重复硬编码厂商话题、QoS、数组下标和安全限制。
- `duojin_perception` 只回答“看到了什么、在哪里、置信度与时效如何”，不得直接驱动机械臂。
- `duojin_navigation` 只负责底盘移动、停靠和到位结果，不做商品识别或抓取编排。
- `duojin_manipulation` 接收经过验证的目标，执行抓放原语并返回结构化结果，不决定整场任务顺序。
- `duojin_task_manager` 只编排能力、处理阶段状态和恢复策略，不包含 YOLO 推理、TF 数学或底层发布循环。
- `duojin_bringup` 只装配节点和参数，不放业务逻辑。
- `duojin_interfaces` 只在标准消息无法表达稳定跨包契约时新增类型，不得成为常量和辅助函数垃圾桶。
- `duojin_arm_test` 不得逐渐演化成比赛主包。稳定能力必须迁入对应生产包，测试包只保留最小诊断工具。

### 4.2 依赖方向

```text
duojin_interfaces
      ↑
robot_interface   perception   navigation   manipulation
      ↑                 由 ROS 契约交换结果，禁止互相导入私有模块
      └────────────── task_manager
                            ↑
                         bringup
```

- `bringup` 可以依赖所有运行包；任何业务包不得反向依赖 `bringup`。
- `task_manager` 可以调用其他包公开的 ROS 契约；其他包不得依赖 `task_manager`。
- 领域包之间优先通过明确 ROS 契约通信，不得导入对方的私有 Python 文件。
- 共享代码只有在至少两个稳定调用方且语义真正相同时才抽取；抽取位置必须有领域名称。
- 禁止循环依赖。

### 4.3 文件放置硬规则

- 根目录不得新增临时 Python、模型、数据、日志或实验脚本。
- `scripts/` 只放项目运维入口；算法与 ROS 节点必须属于 `src/` 中的包。
- 参数放在拥有该参数语义的包内；跨模块比赛配置由 `duojin_bringup/config/` 装配。
- launch 文件只放 `duojin_bringup/launch/` 或确有独立启动价值的所属包 `launch/`。
- 单元测试放所属包的 `test/`；真机记录放 `docs/test-records/`，不得混在源码旁。
- 数据集、rosbag、模型权重、TensorRT engine 和运行日志默认不进普通 Git。需要版本化时使用明确的制品仓库或 Git LFS 方案，并先获得用户同意。
- 禁止使用 `misc.py`、`common.py`、`helpers.py`、无领域限定的 `utils.py`、`temp.py`、`test2.py` 等含糊文件名。
- 禁止复制粘贴另一个节点后仅改话题名；应提取有类型和配置约束的公共实现。

## 5. 代码设计硬规则

### 5.1 纯逻辑与 ROS 分离

- 几何计算、候选筛选、状态机转移、重试决策和配置校验应当是可在无 ROS 环境测试的纯 Python/C++ 逻辑。
- ROS Node 只负责参数、订阅/发布、时间、TF、生命周期和把消息转换为领域类型。
- 不得把全部逻辑写进回调函数或一个巨型 Node 类。

### 5.2 契约必须显式

- 变量和接口名体现单位与坐标系，例如 `offset_z_m`、`yaw_rad`、`point_base`。
- 所有 Pose、图像、点云和检测结果必须保留 `frame_id` 与时间戳；转换前检查 TF 时效。
- 配置必须声明类型、默认值、合法范围和用途；运动参数禁止散落为魔法数字。
- 对外结果至少区分 `SUCCESS`、`RETRYABLE_FAILURE`、`FATAL_FAILURE`、`CANCELLED`、`TIMEOUT`，并携带原因。
- 等待反馈必须有超时；不得无限等待、无限重试或在异常后继续发布旧目标。
- 每个重试都要有上限和变化策略；不能原样无限重做。

### 5.3 控制复杂度

- 一个包只有一个领域职责，一个节点只有一个运行职责，一个函数只做一个可命名步骤。
- 新的 Python 模块超过 400 行或函数超过 60 行时，合入前必须按职责拆分；确实无法拆分的例外要在 `docs/architecture/` 记录理由。
- 禁止用全局可变状态在节点间隐式共享信息。
- 禁止吞掉异常、裸 `except`、只打印错误后继续运动。
- 禁止为了未来可能用到而建立抽象层；接口在出现真实第二个实现或稳定边界时再抽取。
- 新依赖必须说明用途、机器人端安装方式、版本约束和离线部署影响。

## 6. 机器人安全硬规则

任何违反本节的实现都不得进入真机执行。

- 所有运动节点默认必须是 dry-run/preview；执行必须由显式参数或操作员动作开启，launch 不得偷偷把执行默认值改为 `true`。
- 发布前检查：SDK 就绪、反馈新鲜、目标有限且在范围内、TF 有效、目标接口存在订阅者、控制话题没有冲突发布者、急停与场地条件由操作员确认。
- 同一执行器在任一时刻只能有一个目标所有者。自主模式前必须确认 `r1lite_teleop` 等遥操作节点不再争用目标话题。
- 底盘、躯干、双臂和双夹爪必须分别限幅；首次能力测试使用低速、单臂、小位移、单动作。
- 导航时机械臂收回安全姿态；精细抓放时底盘停止并按需要制动。未经专项设计不得同时移动底盘和机械臂。
- 底盘速度命令按已验证频率持续发布；正常结束、取消、超时和异常路径都必须显式发送零速度，并按任务要求恢复制动。
- 机械臂目标必须有到位反馈或超时判定，不能把“消息已发布”写成“动作已完成”。
- 感知结果超过新鲜度上限、深度无效、TF 不可用、置信度不足或目标不可达时禁止执行运动。
- 测试期间操作员必须清场并握住急停。AI 可以给出命令和检查项，但不得把软件检查描述成碰撞安全证明。
- 禁止在未经用户明确授权和真机前置检查时自动运行运动命令。

## 7. AI 工作纪律

AI 在每次任务中必须：

1. 先读本文件、任务相关文档、现有实现和 `git status`；保留用户已有改动。
2. 复述最终结果、约束和验收方式，再决定文件，不凭名称猜实现。
3. 搜索是否已有同职责代码、参数、接口或测试，避免重复建设。
4. 只修改实现任务所需文件；发现旁支问题时记录，不擅自扩大范围。
5. 用证据区分“文档声明”“代码推断”“工控机观测”“真机验证”。
6. 先验证纯逻辑与 dry-run，再建议真机动作。
7. 交付时列出改动、已执行验证、未执行验证和剩余风险。

AI 禁止：

- 修改 `../galaxea/install_430`、`build/`、`install/` 或 `log/`；
- 编造不存在的 SDK API、话题、消息字段、QoS、坐标系或验证结果；
- 用绝对开发机路径写入源码；
- 用当前工作目录偶然可用的相对路径定位 ROS 包资源；
- 把试验代码塞进生产节点、根目录或现有测试包后长期不归档；
- 同时重构无关代码、升级依赖和实现功能；
- 未经请求执行 Git push、创建 PR、更新工控机或发送真机运动命令；
- 为追求“完整”创建大量空包、空目录和占位抽象。

## 8. Git 与交付规则

- 一个提交对应一个可说明的能力、修复或文档变更；不要混入无关格式化。
- 工控机工作区有未提交修改时禁止自动拉取；更新只使用 fast-forward 流程。
- 比赛使用的版本应记录 Git commit、SDK `package_info.txt` 版本和配置版本。
- 行为变化必须同步更新对应 feature、interface、runbook 或 README，不得让代码成为唯一说明。
- 提交前至少检查适用项：

  ```bash
  git diff --check
  bash -n start.sh stop.sh scripts/*.sh
  python3 -m compileall <本次修改的纯 Python/ROS Python 源码>
  python3 -m pytest <本次修改包的本地可运行测试>
  ```

  不能在本地执行的命令必须明确说明并移交工控机验证，不能用无关测试替代。

## 9. 默认决策准则

当存在多种技术方案且用户没有指定时，依次选择：

1. 能在当前赛制和固定场景中稳定验收的；
2. 失败后能安全停止并恢复的；
3. 能在本地测试大部分逻辑的；
4. 与厂商 SDK 耦合最小的；
5. 文件所有权和依赖方向最清楚的；
6. 最少新增依赖和运行进程的；
7. 实现最简单的。

若选择会改变比赛策略、控制安全边界、目录顶层结构或 SDK 依赖方式，必须先向用户说明取舍，不得自行决定。
